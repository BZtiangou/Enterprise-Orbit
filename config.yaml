server:
  port: 8080
  env: development
  name: "Enterprise-Orbit"

database:
  host: "localhost"
  port: 5432
  user: "admin"
  password: "password"
  dbname: "enterprise_orbit"
  sslmode: "disable"

jwt:
  secret: "your-jwt-secret-key"
  expires: 24h

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

log:
  level: "info"
  format: "json"